# Three-Minute Demo Script

This script follows the verified credential-free path. Do not narrate the deterministic candidate steps as completed live GPT calls. Keep both local surfaces running, reset the workflow, close unrelated tabs, and record at a readable desktop scale before starting the timer.

## 0:00–0:20 — why this matters

**Screen:** Admin overview and `원자료 등록부`.

**Narration:**

> 임대 업무에는 안내서, 조감도, 평면도, 면적표, 계약 문서가 계속 새 버전으로 들어옵니다. LeaseFlow는 이 자료를 자산으로 등록하고, AI와 자동화는 후보만 제안하며, 코드와 담당자가 운영에 쓸 현재 정보를 결정합니다.

**Show:** Different synthetic source categories, provenance fields, shareability state, and the manual-review state for unsupported design files.

## 0:20–0:58 — Data Steward to Senior publication

**Screen:** Admin `자산 정보` workflow.

1. Set `검토 담당자` to `Mina Lee · 데이터 담당자`.
2. Click `변경안 4건 찾기`.
3. Point to the source-linked changes: 300 py → 200 py, floor plan v1 → v2, rent-free 3 → 2 months, and supported parking 3 → 2.
4. Click `변경안 확인 완료`.
5. Switch to `Daniel Park · 선임 검토자` and click `승인하고 게시하기`.

**Narration:**

> 후보는 공식 정보가 아닙니다. 데이터 담당자가 출처를 확인하고, 선임 검토자가 게시해야 현장에서 사용할 수 있습니다. 게시 후에도 이전 버전과 승인 기록은 남습니다.

## 0:58–1:38 — current Mobile package

**Screen:** Mobile Web `고객 요청`.

1. Point to `임대 관리 책임자` and `모의 발송 전용` badges.
2. Click `예시 요청 불러오기`, then `요청 확인`.
3. Click `안내 자료 만들기`.
4. Show the current 200 py record and floor plan v2 in the review panel; point out that superseded v1 is excluded.

**Narration:**

> 모바일은 게시 권한이 없습니다. 현재 게시된 200평 정보와 평면도 v2만 조회해 안내 자료를 만들고, 오래된 v1은 선택할 수 없습니다.

## 1:38–1:58 — approval and sandbox delivery

1. Review the configured recipient and attachment.
2. Click `안내 자료 승인`.
3. Click `확인하고 발송하기`.

**Narration:**

> 받는 사람은 모델이 만들지 않고 설정된 그룹에서 옵니다. 마지막 승인 뒤에도 실제 이메일은 보내지 않으며, 이 데모는 샌드박스 전달 기록만 남깁니다.

## 1:58–2:34 — weekly landlord report

**Screen:** Mobile `주간 보고`.

1. Click `건물별 보고서 초안 만들기`.
2. Choose `협의 면적 변동 확인`.
3. Review the evidence-backed proposal and click `제안 반영`.
4. Click `보고서 승인`, then `확인하고 발송하기`.

**Narration:**

> 같은 승인된 자산 정보와 이번 주 샌드박스 활동으로 건물별 보고서를 준비합니다. 제안은 근거와 변경 범위가 보이고, 사용자가 반영 여부와 최종 전달을 다시 결정합니다.

## 2:34–2:50 — the hackathon meaning

**Screen:** Admin version history or Mobile package evidence.

**Narration:**

> 이 데모가 증명하는 것은 자율 발송이 아닙니다. AI는 제안하고, 코드는 권한과 최신 버전을 강제하며, 책임 있는 사람이 승인합니다. 출처와 현재 버전이 운영 오류를 막는 제품 기능이 됩니다.

## 2:50–3:00 — evidence and boundaries

**Screen:** README verification snapshot, then the real Codex session evidence capture if available.

**Narration:**

> Codex로 두 화면과 공통 통제, 테스트를 구현했습니다. 126개 테스트와 다섯 워크스페이스 타입 검사를 통과했습니다. 검증 영상은 합성 데이터와 결정론적 데모이며, 실제 GPT·이메일·Outlook 연동을 주장하지 않습니다.

Show session ID `019f7335-4b59-7e81-8131-b31800757887`. Use a real `/feedback` UI capture only if the submission owner has produced it; never substitute a fabricated URL or screenshot.

## Recording acceptance

- Total duration is 3:00 or less.
- Narration is audible and no private desktop notifications appear.
- The video shows both role changes, current v2 selection, two human approvals, and sandbox-only wording.
- No real landlord file, customer information, credential, private repository control, or local filesystem path is visible.
- The local narrated artifact is 3:00 or less; automated OCR confirms synthetic markers and zero generic secret-pattern matches, and manual sampled-frame checks pass. Do not claim a sensitive-identifier scan unless an explicit out-of-repository blocked-term file was configured for that run.
- Public upload remains a separate external action: verify signed-out playback before adding its URL to the submission.
